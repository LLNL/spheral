### Used to run c++ tests through python for ats
#ATS:test(SELF, label="CXX unit tests")
import subprocess

assert subprocess.call(["@SPHERAL_TEST_INSTALL_PREFIX@/tests/unit/CXXTests/@test_name@"]) == 0
