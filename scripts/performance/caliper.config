# [aggregate-report]
CALI_SERVICES_ENABLE=aggregate,event,mpi,mpireport,timestamp,adiak_import
CALI_TIMER_INCLUSIVE_DURATION=true
CALI_MPIREPORT_CONFIG="select max(sum#time.duration) as MAX, avg(sum#time.duration) as AVG group by prop:nested format tree(print-globals)"
