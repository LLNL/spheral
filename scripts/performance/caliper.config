# [aggregate-report]
CALI_SERVICES_ENABLE=aggregate,event,mpi,mpireport,timestamp,adiak_import,timer
CALI_MPIREPORT_CONFIG="select max(sum#time.duration.ns) as MAX, avg(sum#time.duration.ns) as AVG group by prop:nested format tree(print-globals)"
