#include "libs.hh"
#include "classes.hh"
#include "headers.hh"
namespace FractalSpace
{
  void split_even(Fractal_Memory& mem,Fractal& frac,const int& splits,
		  const vector <double>& xlow,const vector <double>& xhigh,
		  const double& fraction,vector <double>& xsplits)
  {
    
  }
}
