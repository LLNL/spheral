#include "libs.hh"
#include "classes.hh"
#include "headers.hh"
namespace FractalSpace
{
  void tree_decomp()
  {
    vector <double> pos(3);
    int length=frac.get_grid
    for(int particle=0; particle < frac.get_number_particles_world(); ++particle)
      {
	Particle* P=frac.particle_list_world[particle];
	P->get_pos(pos);
      }
  }
}
